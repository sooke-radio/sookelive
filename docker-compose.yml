services:
  payload:
    build:
      context: .
      args:
        MIGRATE: false   # run payload migrate
    ports:
      - ${APP_PORT}:3000
    volumes:
      - web_media:/app/public/media
    env_file:
      - .env
    restart: always
volumes:
  web_media: